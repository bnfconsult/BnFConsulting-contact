// =====================================================================
// PLACEHOLDER — migration React/Vite en cours.
// =====================================================================
// Le site live actuel (bnf-consulting.fr) reste servi en HTML statique
// depuis la racine du repo via GitHub Pages.
//
// Quand la migration des pages commencera, décommenter le code ci-dessous
// et créer src/App.tsx avec le routeur principal.
// =====================================================================

// import { StrictMode } from "react";
// import { createRoot } from "react-dom/client";
// import "@/assets/css/style.css";
// import App from "./App";
//
// createRoot(document.getElementById("root")!).render(
//   <StrictMode>
//     <App />
//   </StrictMode>
// );

export {};
